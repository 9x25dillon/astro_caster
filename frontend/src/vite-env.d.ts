/// <reference types="vite/client" />
// Vite's ambient types: CSS/asset side-effect imports, import.meta.env, ?url
// modules. TypeScript 7 hard-errors (TS2882) on unresolvable side-effect
// imports, so this file — part of the standard Vite template that this
// project never had — became load-bearing with the 7.0.2 bump.
