# Real values for the live origin. GITIGNORED — this repo is public and every
# DNS record is proxied precisely so the origin address stays unpublished.
# Copy to ops/origin.env and fill in.
ORIGIN_IP=
